###commands to run to set up the project##

$ npm init 
-fill all the details in it

$ npm install grunt-cli -g

then open the current folder in the commandprompt like go to that path on the terminal and run the below command

$ npm install grunt --save-dev